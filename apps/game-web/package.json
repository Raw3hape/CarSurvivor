{
  "name": "carsurvivor-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "e2e": "playwright test",
    "verify": "tsc --noEmit && vitest run && vite build",
    "world": "node scripts/build-world.mjs"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "0.20.0",
    "three": "0.186.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/three": "0.185.4",
    "typescript": "7.0.2",
    "vite": "8.3.0",
    "vitest": "4.1.10"
  }
}
