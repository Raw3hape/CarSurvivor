# CarSurvivor web lab

Fast 3D prototypes in the browser. Unity stays the shipping engine.

Read `docs/WEB.md` at the repo root. Run from this folder:

```
npm install
npm run dev
```

Open `http://127.0.0.1:5173`. Clay Earth: pick origin, tap to glaze, unlock neighbors.
